// make an api route that you hit to get the session history

// make a util that gets you the session history

// this component should load the last 10 conversations

// it doesn't need to be client side

// shows each conversation title which is for now the first user prompt

// it should recieve the conversation history as a promp to follow declaritve way

// it should switch the to the relevant conversation one one of it's items are clicked
